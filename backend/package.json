{
  "name": "look-out-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express + Socket.IO realtime server serving Iloilo City LPTRP route data and live PUV tracking",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "engines": {
    "node": ">=18"
  }
}
